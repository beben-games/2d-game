# Milestone 5 Implementation Plan: rounds and the grounds

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** A run is eight rounds in one arena with the crowd's favour, coins, a fall and a verdict, then a return to a grounds room where money buys training and the gate starts the next run; not one word of explanation anywhere.

**Architecture:** `SeriesDef` and `RoundDef` replace floors and rooms; one `Room` lives for the run and its wave runner takes the next round's table, so `Door` and the fade between rooms go. A `Favour` node under Main scores named acts through the pure `FavourRules`; coins are `EnemyDef` data flown to the HUD or thrown as `CoinPile`s; the `Profile` autoload owns a `Save` (money, training, flags, stats, a run log) and listens to the bus for the stats. The fall ends in a verdict scene and the `GateScreen`; the `Grounds` scene sits where the `Room` does, with three stations that open panels. Everything obeys the colosseum design's "show, don't tell".

**Tech Stack:** Godot 4.7.2, GDScript, gdUnit4 (`tools/test.sh`), the smoke tool (`tools/smoke.sh`), ffmpeg for nothing here (the sounds are in).

**Design:** `docs/plans/2026-09-25-milestone-5-design.md` (the intent and the numbers) under `docs/plans/2026-09-25-colosseum-design.md` (the direction). This plan is in the short form: files, behaviour, tests; the committed code is the reference once a task lands.

---

## Read this first

- Every command runs from the repo root, by absolute path `/Users/benjaminzigh/Claude/2d-game` (the desktop harness sometimes drops a session into a stale `.claude/worktrees/...` checkout: ignore it, work and commit in the main checkout with `git -C`). `tools/test.sh` runs all suites and prints a digest (`-a res://tests/<file>.gd` one suite, `-v` the full stream, `reports/test.log` the log); `tools/check_boot.sh` boots headless and runs the reload probe; `tools/smoke.sh <scenario>` needs a display. Run the touched suites during TDD, the full suite once before each commit, and a second time only for a change that can flake (a scene test, a timing, seeded randomness).
- Commit with `git -c user.name="Benjamin Zigh" -c user.email="78459259+beben-games@users.noreply.github.com" commit`, message ending `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`. Commit generated `.uid` and `.import` files (`tools/test.sh` runs `--import` first, so run it before `git add -A`). Never commit a file under `assets/` that `.gitignore` lists; the sounds are already in place and ignored.
- GDScript here is fully typed: `for name: String in [...]`, `var half: int = ...` for integer division or `Dictionary.get`, `var label: Label = auto_free(...)`, duck typing through `get("prop")`, `set`, `has_method`, `call`. A `Resource` subclass with a `class_name` used as a typed `@export` in another def needs the qualified enum form (`ArenaGrid.Side`).
- Physics callbacks: `round_cleared` and `enemy_died` arrive from a shot's `body_entered`. A handler that adds or frees physics nodes, or pauses the tree, defers (`call_deferred`) or awaits a real-time timer first; Main's `_offer_upgrade_later` is the pattern. Adding a `Sprite2D`, a `Node2D`, or a `CPUParticles2D` there is fine.
- Pausing: menus are `CanvasLayer`s with `process_mode` ALWAYS that set `get_tree().paused`; call `Juice.reset()` before pausing. `physics_frame` fires under a pause, so `await ticks(n)` works; real-time timers are `create_timer(t, true, false, true)`.
- Audio: a sound is a row in `data/audio.json` played by the `Audio` autoload on a bus signal; `audio.gd` never names a gameplay class (duck-type through `get()`); `Audio.plays[name]` counts plays for tests. A new sound is a row plus a handler line; the files for this milestone are already in `assets/sfx` and `assets/music` (see `docs/ASSETS.md`), so `test_every_listed_sound_file_exists` must stay green.
- Icons and sprites by name only: `SpriteAtlas` (the tileset, `data/atlas.json`) and `IconAtlas` (`data/icons.json`). The tileset has `coin_anim` (4 frames), `crate`, `column`, `wall_banner_*`, `weapon_*`, `chest_*`, `doors_leaf_closed`, `doors_frame_*` (what `Door` draws). Never hardcode a sheet coordinate.
- **Show, don't tell** (the colosseum design): no caption, hint, tutorial, or label that explains a mechanic; a panel shows its objects, a meter has no name, the gate screen names the gate. The spec-compliance review of every task checks this rule as a spec item.
- Names: "round" replaces "room" in every new or touched identifier, signal, print, and test name; `Room` stays the scene and class name of the arena container (it is one place now). `RunState.round_index` (a bare `round` shadows the built-in), `rounds_cleared`, `rounds_total`; `Events.round_started`, `round_cleared`.
- Record every deviation from this plan or the design in "## Deviations" as it happens ("Task N:" prefix) and add a dated line to "## Progress" at the end of every task; mirror design-level departures in the design doc's "## Deviations during the build".

---

### Task 1: One arena of rounds

**Files:**
- Create: `scripts/defs/series_def.gd` (`SeriesDef`: `arena_width` 28, `arena_height` 15, `rounds: Array[RoundDef]`, `validate()`, `is_last(index)`), `scripts/defs/round_def.gd` (`RoundDef`: `waves: WaveTable`, `validate()`), `data/series/tier_1.tres`, `data/rounds/round_1..8.tres` (the wave tables of `data/rooms/room_N.tres`, unchanged), `scripts/emperor_box.gd` (a `Node2D` drawing the closed door sprites `Door` drew at the top gap: the box until M8's art; `position_of(width, height)` static), `tools/smoke_series.tres`, `tools/smoke_boss_series.tres`, `tests/test_series_def.gd`, `tests/test_series_scene.gd` (from `test_floor_scene.gd`).
- Delete: `scripts/defs/floor_def.gd`, `scripts/defs/room_def.gd`, `scripts/floor_rules.gd`, `scripts/door.gd`, `data/floors/`, `data/rooms/`, `tools/smoke_floor.tres`, `tools/smoke_boss_floor.tres`, `tests/test_floor_rules.gd`, `tests/test_floor_scene.gd`, and their `.uid` files.
- Modify: `scripts/room.gd` (no doors: `arena.build(width, height, [])`, the `EmperorBox` child; `entry_position()` is the centre; `seal_entry`, `open_exit`, `exit_position`, `exit_door` go), `scenes/room.tscn` (the `Doors` node goes), `scripts/main.gd` (below), `scripts/autoload/run_state.gd` (the renames), `scripts/autoload/events.gd` (`round_started(index, total)` replaces `room_entered`; `round_cleared` replaces `room_cleared`; `room_exit_requested`, `door_opened`, `door_sealed` go), `scripts/ui/hud.gd` ("Round n/8"), `scripts/fx.gd` and `scripts/autoload/audio.gd` (the door handlers and rows go; `door_open`/`door_seal` rows leave `data/audio.json`, the files stay), `scripts/arena.gd` and `scripts/arena_grid.gd` (`door_sides` stays as a build parameter, always empty from Room now; nothing else), `tests/support/scene_suite.gd` (`quiet_main_with_series`, `tiny_series(count)`, `boss_series()`; `clear_and_pick` waits the picker and picks), `tools/smoke.gd` (`round` replaces `room`; `SMOKE_ROUND n` after the gap), `tools/smoke.sh` (the scenario names), every test that names a floor, a room def, or a door (`test_room_scene`, `test_arena_scene`, `test_arena_grid`, `test_wave_defs`, `test_summary_scene`, `test_hud_scene`, `test_upgrade_menu_scene`, `test_audio_events_scene`, `test_fx_scene`, `test_boss_stage_scene`: grep `FloorDef|RoomDef|room_cleared|room_entered|door` under `tests/`).

**Behaviour.** Main builds one `Room` at `_ready` (and at `play`) from `series_def` (an `@export`, default `preload("res://data/series/tier_1.tres")`) and keeps it for the run; `_enter_round(index)` sets `RunState.round`, emits `round_started`, and calls `room.wave_runner.start(series_def.rounds[index].waves)`. On `round_cleared`: `rounds_cleared += 1`, the projectiles cleared deferred as today; the last round goes to `_win()`; otherwise the picker after `PICKER_DELAY` as today. After the pick that owes nothing: `upgrade_menu.close()`, then `_next_round_later()`: a real-time `ROUND_GAP` (1.0 s, guarded on `_run_serial` and `_ended` like the picker), then `_enter_round(round_index + 1)`. A round with nothing to offer (an empty pool) goes to the gap at once. The `Fade` stays for the fall (Task 5). The entry seal, `_transition_to`, `_open_exit`, `room_open`, `_transitioning` go; `build_screen.blocked` loses `_transitioning`. The spawner's stream keys on `RunState.round` as it keyed on the room (`spawn:%d`), so placement per round is unchanged; note that `Spawner._ready` runs once now, so it re-seeds in a `start_round()` call from Main (`_rng = RunState.stream("spawn:%d" % RunState.round)`).

**Tests** (`tests/test_series_def.gd`, pure): an empty series fails validation; a null round fails with its index; `is_last` on the last index only; the shipped `tier_1.tres` validates with eight rounds of 28x15. `tests/test_series_scene.gd` (scene, from `test_floor_scene.gd`): a cleared round (emit `round_cleared`) opens the picker after the beat; a pick then a `ROUND_GAP + 0.1` real wait emits `round_started(1, n)` and the runner holds round 2's table (`room.wave_runner.progress.table == series.rounds[1].waves`); the `Room` node is the same instance across the two rounds; the last round's clear wins (`run_won`); a restart during the gap does not start the next round (the serial guard); `RunState.rounds_cleared` counts. `tests/test_room_scene.gd`: the room has no `Doors` child, its walls are solid on every side (a body at the top gap's centre is inside a wall collider: `arena.full_rect()` minus `bounds()`), and `EmperorBox` sits at `EmperorBox.position_of(28, 15)`. Every suite green; `tools/check_boot.sh` ok; `tools/smoke.sh round` prints `SMOKE_ROUND 1`.

**Steps.** (1) Write `test_series_def.gd` and the new `test_series_scene.gd`; run them: they fail to parse (no `SeriesDef`). (2) Add the defs, the data files, the helpers in `scene_suite.gd`; run the def suite green. (3) Rewrite Room, Main, Events, RunState, HUD, Fx, Audio, the smoke tool; delete the old files; fix every failing suite by renaming, never by weakening an assertion. (4) `tools/test.sh` (twice: scene tests changed), `tools/check_boot.sh`, `tools/smoke.sh round`, `tools/smoke.sh boss`. (5) Commit: `rounds: one arena for the run; SeriesDef and RoundDef replace floors and rooms; the door walk and Door go`.

### Task 2: Favour

**Files:**
- Create: `scripts/favour_rules.gd` (pure: the acts table, `band(value)`, `dash_through_danger(from, to, enemy_positions, radius)`, `cowardice(idle_seconds, delta)`), `scripts/favour.gd` (a `Node` under Main, `Favour`, the detectors on the bus), `tests/test_favour_rules.gd`, `tests/test_favour_scene.gd`.
- Modify: `scripts/autoload/run_state.gd` (`favour: float`, `perfect: bool`, `hits_this_round: int`, reset by `start_run`), `scripts/autoload/events.gd` (`favour_changed(value: float, band: int, act: String)`, `round_ended(band: int)`), `scenes/main.tscn` (the `Favour` node), `scripts/main.gd` (emits `round_ended(band)` before the picker beat; passes the granter and the count to the menu; clears `hits_this_round` at `round_started`), `scripts/ui/upgrade_menu.gd` (`open(offers, granter := "")`: a title label over the cards with the granter's name, a fourth card and `pick_4`), `project.godot` (`pick_4` on key 4), `scripts/upgrade_catalog.gd` (`offers(build, hurt, rng, count := 3)`; the heal slot is still the last), `scripts/ui/hud.gd` (the meter under the hearts: a nine-patch bar `UiTheme.BAR` if the sheet has one, else a framed `ColorRect`, filled to `favour / 100` in the band's colour, no label), `data/audio.json` and `scripts/autoload/audio.gd` (`round_ended` band → `crowd_boo|quiet|cheer|roar`; the four rows at -6 dB, gap 0.5).

**Behaviour.** Constants in `favour_rules.gd`: `START 30.0`, `MAX 100.0`, `ACTS := {"kill": 3, "chain": 2, "daring": 3, "clean_round": 15, "hit": -20}`, `CHAIN_WINDOW 1.5`, `DASH_WINDOW 0.5`, `DANGER_RADIUS 24.0`, `IDLE_GRACE 4.0`, `COWARDICE_PER_SECOND 2.0`, bands `BOO 0, QUIET 1, CHEER 2, ROAR 3` with edges 25/50/75 (`band(25.0) == QUIET`, `band(74.9) == CHEER`). The `Favour` node: on `enemy_died` scores `kill`, `chain` when within the window of the last kill (`Time.get_ticks_msec` is wall time; use `RunState.elapsed`, which stops under a pause), `daring` when a dash through danger ended within `DASH_WINDOW`; on `player_dashed(position, direction)` computes the dash segment (`DashRules.SPEED * DashRules.DURATION`, whatever the names are in `dash_rules.gd`) and asks `dash_through_danger` against every live, harmful enemy's position (`get_tree().get_nodes_in_group("enemies")`, `is_harmful()`), remembering the time when true; on `player_hit` scores `hit`, sets `perfect` false, `hits_this_round += 1`; on `round_cleared` scores `clean_round` when `hits_this_round == 0`; each physics tick, when any live enemy is harmful and `RunState.elapsed - last_engagement > IDLE_GRACE`, drains `cowardice` (`last_engagement` is the time of the last `enemy_hit` or `enemy_died`; reset at `round_started`). Every change clamps to 0..100 and emits `favour_changed(value, band, act)`. `RunState.perfect` starts true and also turns false when a round ends below ROAR. Main, at the round's end: `Events.round_ended.emit(band)` (before the projectiles clear), the picker gets `granter` "The crowd" for CHEER and ROAR, "The emperor" for BOO and QUIET, and `count` 4 on ROAR. The menu draws the fourth card at the same size (the cards row shrinks its gaps; `CARD_SIZE` stays), and `pick_4` chooses it.

**Tests** (`test_favour_rules.gd`): every act's value; the bands at the edges; `dash_through_danger` true for an enemy 20 px off the segment's middle, false at 30 px and false for an enemy behind the start; `cowardice(3.9, 1.0) == 0.0` and `cowardice(4.0, 1.0) == -2.0`; clamping at 0 and 100. `test_favour_scene.gd`: a kill raises favour by 3 and emits `favour_changed` with act "kill"; two kills within 1.5 s score 5 then 5; a hit drops 20 and clears `perfect`; a dash through a placed chaser then a kill within 0.5 s scores "daring"; a dash in the open then a kill does not; a round cleared with no hit scores 15; four seconds beside a live chaser without shooting drains 2 in the fifth second (advance `RunState.elapsed` by ticks: 60 Hz, so 300 ticks); `round_ended` emits the band and plays the band's crowd sound (`Audio.plays["crowd_cheer"] == 1` after setting favour to 60); at 80 favour the picker opens with four cards, a granter label "The crowd", and `pick_4` takes the fourth. `test_upgrade_catalog.gd`: `offers(..., count 4)` gives four distinct cards with the heal card last when hurt. `test_hud_scene.gd`: the meter's fill ratio follows `favour_changed`.

**Steps.** Tests first (rules, then scene), red; the rules class; the node and the wiring; the menu and the HUD; the audio rows; full suite twice (scene tests and timing); `tools/smoke.sh pick` and read `reports/smoke_pick_menu.png` for the granter label. Commit: `favour: the crowd scores named acts; bands, the granter and the fourth card on a Roar; the meter on the HUD`.

### Task 3: Coins

**Files:**
- Create: `scripts/pile_rules.gd` (pure: `split(total, count)`, `pile_count(total)` 4..8, `spots(center, radius, count, bounds, rng)`), `scripts/coin_pile.gd` and `scenes/coin_pile.tscn` (an `Area2D`, layer 32, mask 65, a `coin_anim` sprite, `value`, the toss tween, `pile_collected` on a body), `scripts/ui/coin_flight.gd` (a `Sprite2D` on the HUD layer tweened from a world position to the counter), `tests/test_pile_rules.gd`, `tests/test_coins_scene.gd`.
- Modify: `scripts/defs/enemy_def.gd` (`coins: int`, validated `>= 0`), `data/enemies/chaser.tres` 1, `chaser_shield.tres` 2, `shooter.tres` 2, `boss.tres` 60 (the `BossDef` extends `EnemyDef`? check; else add it there too), `scripts/autoload/run_state.gd` (`coins: int`, `round_tally: int`), `scripts/autoload/events.gd` (`coins_changed(run_coins: int)`, `coins_thrown(position: Vector2, total: int)`, `pile_collected(position: Vector2, value: int)`), `scripts/ui/hud.gd` (the counter: a `coin_anim` icon at 3x and a number, top right; `CoinFlight`s land on it), `scripts/main.gd` (the round's bonus: after `round_ended`, CHEER adds `tally / 2` to the counter through one flight from the box, ROAR throws `tally` as piles around the player; the boss's death throws its coins at its position; `throw_piles(at, total)` uses `RunState.stream("piles:%d" % RunState.round)` and the room's `bounds()`), `scripts/room.gd` (`piles: Node2D` container; piles are freed with the room, so a new run has none), `data/audio.json` and `audio.gd` (`coin_get` on a flight's arrival, `coin_toss` on `coins_thrown`, `coin_pickup` on `pile_collected`; `coin_get` at -12 dB with a 0.03 gap, jitter 0.1).

**Behaviour.** On `enemy_died`, Main (or the HUD, listening) reads the enemy's `def.coins` (duck-typed, 0 when absent), adds it to `RunState.coins` and `round_tally`, emits `coins_changed`, and starts a `CoinFlight` from the death position; the flight is cosmetic (the counter already holds the sum; the flight's arrival plays `coin_get`). `CoinPile.toss(from, to)` tweens an arc (0.4 s, a parabola of 24 px) and enables monitoring on landing; contact with the player body (layer 1 or 64) adds `value`, emits `pile_collected`, frees the pile. `PileRules.spots` picks seeded points within `PILE_RADIUS` (64) of the centre, clamped inside `bounds` shrunk by 8 px, at least 12 px apart (retry up to 20 times per pile, then accept). A run's coins reach the profile only in Task 5.

**Tests** (`test_pile_rules.gd`): `split(10, 4) == [3, 3, 2, 2]`; `pile_count` 4 at 8 coins, 8 at 60; `spots` are inside the bounds, within the radius, and seeded (the same stream gives the same list). `test_coins_scene.gd`: a chaser killed by `Health.take_damage` adds 1 to `RunState.coins` and emits `coins_changed(1)`; a flight node appears on the HUD and is gone after its tween (a real 0.5 s); `throw_piles(center, 12)` adds `pile_count(12)` piles under `Room/Piles` whose values sum to 12, emits `coins_thrown`; a pile the player walks onto (place the player on it, one tick) adds its value and frees itself; a dashing player collects too (layer 64: set the player dashing, then place); a round ended at CHEER adds half the tally; at ROAR the tally's piles appear and the counter is unchanged until collected; the boss's death throws 60. `test_enemy_def.gd`: `coins` validated.

**Steps.** Rules tests, red, the rules; the scene tests, red, the pile scene, the flight, the wiring, the data; full suite twice; `tools/smoke.sh kill` and read the PNG for the counter. Commit: `coins: EnemyDef.coins fly to the HUD counter; piles thrown on a Roar and at the boss's death, collected by walking or dashing`.

### Task 4: The profile and the save

**Files:**
- Create: `scripts/save.gd` (`Save`, `RefCounted`: `VERSION 1`, `DEFAULT_PATH "user://save.cfg"`, `STAT_KEYS`, `RUN_LOG_CAP 500`, `load_from(path)`, `save_to(path)`, `add_stat(key, amount, id := "")`, `stat(key, id := "")`, `log_run(record)`), `scripts/autoload/profile.gd` (`Profile`: holds `save`, `path` (tests set a scratch path then `reload()`), listens to the bus for the stats, `reset()`), `tests/test_save.gd`, `tests/test_profile_scene.gd`.
- Modify: `project.godot` (the `Profile` autoload after `Audio`), `scripts/player.gd` (`hurt(damage, from, attacker_id := "")`; `_check_contact` passes `def.get("id")` for a body and the bolt's `shooter_id` for a bolt), `scripts/projectile.gd` (`shooter_id: String`, set by `Enemy._fire_bolt` and `Boss._fire_bolt` from their def id), `scripts/autoload/events.gd` (`player_hit(damage, hp, max_hp, attacker_id)`; `player_died` gains `attacker_id` too, renamed in Task 5), `tests/support/scene_suite.gd` (`PROFILE_SCRATCH := "user://test_profile.cfg"`; `after_test` calls `Profile.reset()` and removes the scratch file), every test that connects `player_hit` (grep).

**Behaviour.** `Save` holds `money`, `training: Dictionary` (line id → rank), `flags` (`runs`, `wins`, `falls`, `deaths`, `perfect_runs`, `returned`), `stats` (a Dictionary keyed by `STAT_KEYS`; a per-id key holds a Dictionary id → int), `runs: Array[Dictionary]` (newest first). `STAT_KEYS` are the design's: `shots_fired` (per weapon), `shots_hit`, `hits_landed` (per enemy), `kills` (per enemy), `hits_taken` (per attacker), `deaths_by` (per attacker), `dashes`, `dashes_through_danger`, `cards_taken` (per card), `switches`, `rounds_cleared`, `rounds_by_band` (per band name), `clean_rounds`, `perfect_runs`, `boss_kills`, `boss_time_best`, `coins_earned`, `coins_lost`, `coins_spent`, `piles_collected`, `favour_peak`, `time_played`, `time_in_grounds`, `best_run` (a Dictionary: rounds, kills, time). `load_from` returns defaults on a missing file or a `version` above `VERSION`; an older version loads what it has (missing keys at their defaults). `save_to` writes a `ConfigFile` with sections `meta`, `money`, `training`, `flags`, `stats`, `runs` (nested Dictionaries and Arrays serialise as Variants). `Profile` listens: `shot_fired` → `shots_fired[weapon_id]`; `enemy_hit` → `shots_hit` and `hits_landed[def id]` (duck-typed); `enemy_died` → `kills[id]`, `boss_kills` for the boss group; `player_hit` → `hits_taken[attacker_id]`; `player_dashed` → `dashes`; `favour_changed` act "daring" → `dashes_through_danger`, and `favour_peak` as a max; `upgrade_chosen` → `cards_taken[card.id]`, `switches` for a SWITCH; `round_cleared` → `rounds_cleared`; `round_ended` → `rounds_by_band[name]`, `clean_rounds` when `hits_this_round == 0`; `pile_collected` → `piles_collected`; `time_played` accumulates in `_process` while unpaused. Nothing is written to disk by the listeners: `Profile.commit()` writes, called by the verdict (Task 5) and the purchases (Task 6). `Profile._ready` loads from `path`; `reset()` reloads from the path (a test's scratch path is missing, so defaults).

**Tests** (`test_save.gd`): defaults on a missing file; a round trip through a scratch file keeps money, a training rank, every flag, every `STAT_KEYS` entry (set each to a distinct value, per-id ones with two ids) and a logged run; a file with `version` 99 loads as defaults; a file missing `stats` loads with zero stats; `log_run` keeps newest first and caps at `RUN_LOG_CAP` (log 502, expect 500, the first logged gone); `add_stat` on a per-id key without an id fails an `assert`. `test_profile_scene.gd`: a shot fired counts under its weapon; a kill counts under the enemy's id; a hit from a placed chaser counts under "chaser" (`hits_taken["chaser"] == 1`) and `player_hit` carries the id; a bolt's hit counts under "shooter"; a card taken counts; nothing is on disk after those (the scratch file does not exist) until `commit()`, after which `Save.load_from(scratch).stat("kills", "chaser") == 1`.

**Steps.** Save tests, red, `save.gd`; the profile tests, red, the autoload and the signal changes; full suite (the `player_hit` signature touches many suites: fix by adding the argument, never by dropping an assertion); commit: `profile: the save file with money, training, flags, every stat, and a run log; the bus fills the stats`.

### Task 5: The fall and the verdict

**Files:**
- Create: `scripts/verdict_rules.gd` (pure: `decide(band, hits_taken, flags, cheats) -> bool` (true is UP), `gate_name(up)`), `scripts/thumb_sign.gd` (a `Node2D` drawing a 16x16 thumb up or down from an `Image` in code, at 3x, over the box; `show_thumb(up)`), `scripts/ui/gate_screen.gd` and `scenes/ui/gate_screen.tscn` (`GateScreen`, CanvasLayer 30, replacing `Summary`), `tests/test_verdict_rules.gd`, `tests/test_gate_screen.gd` (pure text, from `test_summary.gd`), `tests/test_fall_scene.gd` (from `test_summary_scene.gd`).
- Delete: `scripts/ui/summary.gd`, `scenes/ui/summary.tscn`, `tests/test_summary.gd`, `tests/test_summary_scene.gd`.
- Modify: `scripts/cheats.gd` (`"verso": {"thumbs_down": true}`, `"dives": {"rich": true}`; `describe` unchanged), `scripts/autoload/events.gd` (`player_fell(position, attacker_id)` replaces `player_died`; `verdict_given(up: bool)`; `run_ended(outcome: String)`), `scripts/player.gd` (`_die` → `_fall`: the sprite laid flat, `rotation = -PI / 2` on the sprite, still visible; `dead` stays the flag name), `scripts/fx.gd` and `audio.gd` (`player_die` row plays on `player_fell` still; `crowd_hush` on `player_fell`; `verdict_up`/`verdict_down` on `verdict_given`; `gate` when the gate screen closes), `scripts/main.gd` (the scene below; `restart()` as a yield; `quit_to_title()` a yield; the print), `scenes/main.tscn` (`GateScreen` in place of `Summary`, `ThumbSign` under `Room`'s box or under Main), `tools/smoke.gd` (`fall` replaces `death`: `SMOKE_VERDICT up`, the gate screen's title), `tools/smoke.sh`, `docs/TESTERS.md` (later, Task 7), tests naming `player_died` or `Summary` (grep).

**Behaviour.** Constants in `main.gd`: `VERDICT_HOLD 1.0`, `VERDICT_SHOW 1.2`. On `player_fell` (guarded by `_ended` as today): `_ended = true`, the runner off, `crowd_hush` (via the bus), a real-time `VERDICT_HOLD`, then `_verdict(false)`. `_win()`: after the boss's corpse hold as today, `_verdict(true)`. `_verdict(won)`: `up = VerdictRules.decide(FavourRules.band(RunState.favour), Profile-independent hits, Profile.save.flags, RunState.cheats)`; in M5 `decide` returns `not cheats.get("thumbs_down", false)` (the band and the flags are parameters so M9 fills the rule; a test pins the signature); the thumb shows over the box, `verdict_given(up)`, a real `VERDICT_SHOW`, the fade to black, then the gate screen. Banking: UP adds `RunState.coins` to `Profile.save.money` and `coins_earned`; DOWN adds to `coins_lost` and counts `deaths` and `deaths_by[attacker]`; both count `runs`, `wins` or `falls`, `perfect_runs` when `RunState.perfect` and won, `best_run`, and `log_run` the record (seed, cheats, outcome, verdict, rounds, kills, time, coins earned and kept, hits, the bands per round (Main keeps `round_bands: Array[int]`), the build, the training ranks, the date); then `Profile.commit()`. The print becomes `RUN_END outcome=win|fall verdict=up|down kills=%d rounds=%d coins=%d seed=%d elapsed=%.1f cheats=...`. The gate screen: the title is `VerdictRules.gate_name(up)`; the body is two blocks from `GateScreen.body(run: Dictionary, save: Save)` (the run's rounds, kills, time, coins earned, coins kept, seed, cheats; all time: runs, wins, falls, deaths, kills, hits taken, shots fired; the deadliest enemy: the id with the most `hits_taken`, shown as its `idle_anim` in an `AnimatedSprite2D` at 4x, or nothing when no hit was ever taken); `ui_accept` or a click emits `continue_requested` (Main goes to the grounds in Task 6; until then it restarts into a new run), Esc emits `quit_requested` as today. **The yield:** `restart()` (R, the pause screen's Restart) ends the run without a verdict: `run_ended("yield")`, `coins_lost += RunState.coins`, `falls += 1`, the record logged, `commit()`, then the reload as today; `quit_to_title()` does the same before the title. `dives` gives `RunState.coins` 1000 at `start_run` (read in `start_run`, the one place). `check_boot`'s `tools/reload_probe.gd` keeps passing (the yield path runs in it).

**Tests** (`test_verdict_rules.gd`): UP for every band with empty cheats; DOWN with `thumbs_down`; `gate_name`. `test_gate_screen.gd`: the body texts for a run and a save (the deadliest id chosen by max, ties by name order). `test_fall_scene.gd`: a fall (hp 1, a chaser beside the player, ticks) sets `player.dead`, lays the sprite flat, plays `crowd_hush`, and after `VERDICT_HOLD + 0.1` the thumb is visible and up, `verdict_given(true)` emitted, `verdict_up` played; after `VERDICT_SHOW + FADE + 0.2` the gate screen is visible with "Porta Triumphalis" and the run's coins are in `Profile.save.money`; with `verso` (`RunState.start_run(-1, {"thumbs_down": true})`) the thumb is down, the title "Porta Libitinaria", `money` unchanged, `deaths == 1`, `deaths_by["chaser"] == 1`; a win (a boss series, kill the boss) reaches the same screen with `wins == 1`; a yield (`restart()` mid-run with 5 coins) logs a record with outcome "yield" and `coins_lost == 5`; `ui_accept` on the gate screen closes it and plays `gate`; the `RUN_END` print (assert through `Audio.plays`? no: assert the flags and the record; the print is read by the smoke tool). Smoke `fall` asserts `SMOKE_VERDICT up`.

**Steps.** Rules and text tests, red, the rules and the screen text; the scene tests, red, the fall scene in Main, the thumb, the gate screen scene, the cheats, the yield; full suite twice; `tools/check_boot.sh`; `tools/smoke.sh fall` and read the PNG (the thumb over the box, the flat gladiator). Commit: `verdict: the fall, the thumb over the box, the gate screen with the run and all-time stats; R and Restart yield; verso and dives cheats`.

### Task 6: The grounds

**Files:**
- Create: `scripts/training_rules.gd` (pure: `LINES` table, `price(line, rank)`, `can_buy(save, line)`, `apply(save, build)`), `scripts/grounds.gd` and `scenes/grounds.tscn` (`Grounds`: an `Arena` built solid 28x15, `Stations: Node2D`, the three `Station` `Area2D`s), `scripts/station.gd` (`Station`: a sprite, an `Area2D` masking 65, `entered`/`exited` signals with the station id), `scripts/ui/training_panel.gd`, `scripts/ui/armoury_panel.gd` (CanvasLayers 10, framed with `UiTheme`, mouse only, `process_mode` INHERIT: the grounds are not paused), `tests/test_training_rules.gd`, `tests/test_grounds_scene.gd`, `tests/test_flow_scene.gd`.
- Modify: `scripts/build.gd` (`static func starting(save: Save) -> Build`: `BASE_MAX_HP + 2 * hearts`, `BASE_DASH_CHARGES + breath`, and `starting_favour` for renown, read by `RunState.start_run` which builds from `Profile.save`), `scripts/autoload/run_state.gd` (`start_run` builds `Build.starting(Profile.save)` and sets `favour` from it), `scripts/main.gd` (`enter_grounds()` and `enter_arena()`: the `Room` or the `Grounds` under Main at index 0, the HUD hidden in the grounds, the camera limits from either; the first run: `play()` from the title goes to the arena when `flags.returned` is false, else to the grounds; the gate screen's `continue_requested` → `enter_grounds()` and `flags.returned = true` + `commit()`; the gate station → `enter_arena()` with a fade and `gate`), `scripts/autoload/events.gd` (`grounds_entered()`, `training_bought(line: String, rank: int)`), `data/audio.json` and `audio.gd` (`music_grounds` on `grounds_entered`, `music_run` on `round_started(0, _)`, `buy` on `training_bought`, `buy_denied` on a refused click: the panel emits `purchase_denied` on the bus), `tools/smoke.gd` (`grounds`: enter the grounds with `flags.returned` forced, walk into the post, `smoke_grounds.png` with the panel up, `SMOKE_GROUNDS post`), `tools/smoke.sh`, `docs/TESTERS.md`.

**Behaviour.** `TrainingRules.LINES := {"hearts": {"ranks": 3, "prices": [50, 100, 200]}, "breath": {"ranks": 2, "prices": [80, 160]}, "renown": {"ranks": 3, "prices": [40, 80, 160]}}`; `apply` gives `{"max_hp": 6 + 2 * hearts, "dash_charges": 1 + breath, "favour": FavourRules.START + 10 * renown}`. The grounds: the player spawns at the bottom centre; three stations: `post` (a `crate` sprite with `weapon_spear` leaning, left third), `rack` (three `weapon_*` sprites on the top wall's face, right third), `gate` (the emperor's box position, top centre, drawn as the open door: `doors_leaf_open` if the tileset has it, else the frame alone). Entering `post` opens the training panel: a framed column of three rows, each an icon (`heart_container` for hearts, `dash_charge` for breath, a coin for renown), rank pips (`HUD`'s pip style), the next price with a coin icon, and the row greyed when capped or unaffordable; a click buys (`save.money -= price`, `training[line] += 1`, `coins_spent`, `training_bought`, `commit()`) or emits `purchase_denied`. No text beyond the number. Leaving the station closes the panel. `rack` opens the armoury panel: the gladiator's idle animation at 4x, the handgun icon in a lit slot, two empty framed slots beside it, nothing clickable. `gate`: the fade, `enter_arena()`, `round_started(0, 8)` and the run. Esc in the grounds opens the pause screen as in the arena (Resume, the volumes, Quit to title, Quit game; Restart is hidden there).

**Tests** (`test_training_rules.gd`): prices per rank; `can_buy` false when capped or poor; `apply` on ranks 0 and max. `test_grounds_scene.gd` (a Main with `enter_grounds()` and a scratch profile): the grounds node replaces the room, the HUD is hidden, `music_grounds` plays; walking the player into the post's area (place and tick) opens the training panel; a click on the hearts row with 60 money buys rank 1, money 10, `training_bought("hearts", 1)`, `buy` played, the file on disk holds it; a click with 10 money emits `purchase_denied` and plays `buy_denied`; walking out closes the panel; the rack opens the armoury panel with a lit handgun slot and two empty; the gate station starts a run: the room is back, `round_started(0, 8)`, the player's `max_hp` is 8 with hearts rank 1 and `RunState.favour` 40 with renown 1. `test_flow_scene.gd`: a fresh profile's `play()` goes to the arena, not the grounds; after the gate screen's continue the grounds are up and `flags.returned` is saved; the next `play()` from the title goes to the grounds. `test_build.gd`: `Build.starting` from a save with ranks.

**Steps.** Rules tests, red, the rules; the grounds scene tests, red, the scene, the stations, the panels, the flow; full suite twice; `tools/check_boot.sh` (the reload probe gains a gate-screen continue and a grounds entry); `tools/smoke.sh grounds` and read the PNG. Commit: `grounds: a walkable room with the training post, the rack, and the gate; training lines bought with money shape the next run; the first run starts in the arena`.

### Task 7: Close the milestone

**Files:** `docs/TESTERS.md` (rewritten: no explanation of any mechanic; the controls, the build's facts, and the questions: what did you understand of the meter, the coins, the thumb, the grounds, and where did you feel told), `docs/STATUS.md`, `CLAUDE.md` (the new conventions: rounds and series, the favour acts, the pickups layer in use by piles, the profile and the scratch path rule, the cheats `verso` and `dives`, the smoke scenarios `round`, `fall`, `grounds`, the show-don't-tell review item), `docs/plans/2026-09-25-m5-feel-checklist.md` (one row per question, the knob for each), `project.godot` (`config/version` 0.5.0-rc1), the design doc's deviations.

**Behaviour.** A read of every task's deviations into the docs; `tools/test.sh` twice, `tools/check_boot.sh`, every smoke scenario; `tools/build.sh` and `tools/release.sh` (a pre-release `v0.5.0-rc1`); `git tag m5-candidate`. The milestone closes on the user's playtest verdict.

**Steps.** Docs; the checks; the build and the release; the tag; commit: `release: 0.5.0-rc1, the M5 candidate`.

---

---

## Playtest 1 tasks (2026-09-25)

The user's sixteen notes on `0.5.0-rc1` are in `docs/plans/2026-09-25-m5-feel-checklist.md`, "Verdict, playtest 1", with the decision per note in its Change column. Four tasks build them, one commit per note, through the same loop; Task 12 closes rc2. The rule "show, don't tell" was refined on this playtest (colosseum design): **UI may name, never narrate**.

### Task 8: The eleven small notes (1, 2, 4, 6, 7, 8, 10, 13, 14, 15, 16)

One commit per note, in this order, each with its test first where behaviour changes:

- **14, a bug.** A run started from the gate has no scene reload, and the HUD's build strip and coin counter kept the previous run's contents. `Hud._on_run_started` refreshes the strip (`_refresh_build`) and the counter (from `RunState.coins`, 0). Test in `test_hud_scene.gd`: a strip holding two upgrades and a counter at 12 are empty and 0 after `RunState.start_run()`.
- **4, the boss.** `data/enemies/boss.tres`: `max_hp` 600, `telegraph_time` 0.5, `recover_time` 0.55, `charge_speed` 380; stage two's own numbers unchanged. `test_boss_def.gd` pins the shipped numbers; `tools/smoke.sh boss` asserts `SMOKE_BOSS_HP n of 600` (update `smoke.sh`, `CLAUDE.md`, `STATUS.md`).
- **7, no verdict on a win.** `Main._verdict(true)` skips the thumb: after `WIN_HOLD`, the sweep, `_bank` as UP (money, wins, perfect, best run, boss time), the fade, the gate screen "Porta Triumphalis"; `verdict_given` is not emitted on a win (the fanfare `verdict_up` still plays: emit a new `run_won_ceremony()`? No: `Audio` maps `run_won` already; move the fanfare to `run_won` and keep `verdict_up` for a thumb up after a fall). `VerdictRules.decide` is called only on a fall. Tests in `test_fall_scene.gd`: the win case asserts no thumb and no `verdict_given`; the fall cases unchanged.
- **8, the money in the panel.** `TrainingPanel` shows the money held at its top: `SpriteAtlas.rect("coin_anim", 3)` and a `UiTheme.label` number, refreshed on open and after every purchase. Test: the label reads "60" then "10".
- **10, the row lines.** Each training row gets a short line under its icon naming what it buys, from `TrainingRules.LINES[line].text` (a new field; the texts land with Task 11's lines; for now Hearts "One more heart", Breath "One more dash", Renown "A warmer crowd"). `UiTheme.FONT` at 32. Test: the row's label text equals the table's.
- **15, the portrait's line.** Under the deadliest enemy: `"<display_name> hit you <n> times"` (the def's `display_name`; `n` the all-time `hits_taken[id]`), `UiTheme.label` at 32. `GateScreen.blocks` unchanged; a new `portrait_line(save) -> String` tested for the text and for "" when no hit was ever taken.
- **16, the bars read.** An icon at the left of the dash pips (`IconAtlas` `dash_charge`, which exists, at 3x) and of the favour meter (a small crowd: check `tools/gen_icons.py`'s Raven table for a crowd, a mask, or a laurel; if none reads as a crowd, draw a 16x16 two-heads placeholder in code as `ThumbSign` does, `PLACEHOLDER` in the doc). Test: the two icons exist as children named `DashIcon` and `FavourIcon`.
- **6, the thumb.** `ThumbSign` draws a 24x24 image: a fist (a rounded block with four knuckle bumps and a cuff line) and a thumb (three pixels wide, six tall) up or down, two skin tones plus an outline, at 2x (48 px on screen), centred over the box with its bottom on the arena's top edge. Test: the image is 24x24 and the placement.
- **13, the fourth card's reveal.** `UpgradeMenu.open(offers, granter, reveal_last := false)`: with four cards and `reveal_last`, the first three build at once and the fourth is added after `FOURTH_CARD_DELAY` (0.6 s, real time, ignoring the pause since the menu is paused: `create_timer(..., true, false, true)`), sliding in from the right edge over `FOURTH_CARD_SLIDE` (0.25 s) while `crowd_roar` plays again (`Events.card_revealed()` → Audio); `pick_4` and the click land only once it is in (`offers.size() == 4` from the start, the button added late). Main passes `reveal_last = count == 4`. Tests: three cards at once; with four, three buttons at open, four after the delay, `pick_4` before the delay does nothing, after it takes the fourth.
- **1, the pull.** `CoinPile`: once landed, each physics tick a pile within `PULL_RADIUS` (96 px) of the player moves toward the player at `PULL_SPEED` (220 px/s, accelerating by `PULL_ACCEL` 600 px/s²) until contact; `Reach` (Task 11) widens the radius through `RunState.pull_radius`, set at `start_run` from the profile (base `PULL_RADIUS`). Tests in `test_coins_scene.gd`: a pile at 80 px reaches the player within 30 ticks and pays; a pile at 200 px does not move.
- **2, the wait.** `Main._next_round_later`: after `ROUND_GAP`, wait until `room.piles` is empty, up to `PILE_WAIT_CAP` (6.0 s real time, not process-always: it pauses with the tree), then `_enter_round`. Tests: with two piles on the floor the next round does not start after `ROUND_GAP + 0.5`; after they are collected it starts within a tick; with piles never collected it starts after the cap.

### Task 9: Favour retune (note 3)

`favour_rules.gd`: `START 20`, `ACTS := {"kill": 1, "chain": 2, "daring": 4, "clean_round": 10, "hit": -25}`, and a **decay** replacing the cowardice drain: `DECAY_GRACE 3.0` s after the last scoring act (a kill, a chain, a daring, a clean round: not a hit), while any enemy is harmful, the meter loses `DECAY_PER_SECOND` 1.5 (`DECAY_ACT := "decay"` replaces `COWARDICE_ACT`). Running away and idling both decay; standing and fighting keeps the meter. Bands unchanged. The `Favour` node's tick uses the last scoring act's time. Tests re-pinned (`test_favour_rules.gd`, `test_favour_scene.gd`); `docs/STATUS.md`'s numbers; the checklist's Change column says the numbers shipped.

### Task 10: The verdict's build-up (note 5)

`Main._verdict(false)` (a fall only, after Task 8's note 7): after `VERDICT_HOLD`, the **build-up**: `crowd_quiet` fades the crowd (Audio: a `verdict_hush_loop`? no new file: play `crowd_hush` then silence), the camera drifts from the player to the emperor's box over `VERDICT_DRIFT` (1.5 s, a tween on the camera's position with the limits released: `Camera2D` `position_smoothing` off, tween `global_position` to `EmperorBox.centre()`), a drum roll (`verdict_drum`, a new row: source it from the user's packs: the "floraphonic percussive drum hit" already in `verdict_down` is a hit, not a roll; ask the user for a roll or loop `crowd_quiet` low; the plan says: a new row `verdict_roll` mapped to `sfx/verdict_roll.wav`, silent until the user supplies it, `AUDIO_MISSING` allowed), a held pause `VERDICT_PAUSE` (1.2 s), then the thumb, `verdict_given`, `VERDICT_SHOW`, the fade, the gate screen. About four seconds from the fall to the thumb. The camera returns to the player on the next run's start (`_forget_run` resets it). Tests: the stages by real-time constants; the camera at the box when the thumb shows; the reload probe and the smoke `fall` waits updated. `CLAUDE.md`'s real-time list and the fall line.

### Task 11: The training lines that a run cannot give (note 11)

`TrainingRules.LINES` becomes: `offer` (one more card in every offer; 2 ranks; 120/240; text "One more card to choose from"), `reroll` (one re-roll of an offer per run; 2 ranks; 100/200; "Change the cards once a run"), `mercy` (the emperor spares you once per run; 1 rank; 300; "Fall once and fight on"), `reach` (a wider coin pull; 3 ranks; 40/80/160; "Coins come from further"). `apply(save)` → `{offer_bonus, rerolls, mercies, pull_radius}`; `RunState.start_run` reads them (`offer_bonus` added to `FavourRules.offer_count`'s result in Main; `rerolls_left`, `mercies_left`, `pull_radius`). The picker gets a **Reroll** button (`UiTheme.button`, under the cards, visible only while `RunState.rerolls_left > 0`; a press redraws the same round's offer from the next stream `upgrades:%d:%d:r%d` and decrements; the heal-slot rule holds). **Mercy**: `Player.hurt` at 0 hp with `RunState.mercies_left > 0` decrements it, sets hp to 2 (one heart), plays `verdict_up`... no: plays `crowd_roar` and emits `mercy_granted(position)` (Fx: a ring flash; Audio: `crowd_roar`), no fall. `Build.starting` loses the hearts/breath offsets (`base_max_hp`/`base_dash_charges` stay as fields at their constants for a later use, or go: go). The icons: `offer` a card (`IconAtlas`: check the table for a scroll or cards; else placeholder), `reroll` (a dice or arrows), `mercy` (`heal` icon), `reach` (a coin). The panel shows four rows. The old ranks in an existing `save.cfg` (`hearts`, `breath`, `renown`) load harmlessly and are ignored (a test). Tests: the rules; the reroll button's draw and count; a fall with a mercy left continues with one heart; `Reach` widening the pull.

### Task 12: Close rc2

The checklist's Done column; `docs/STATUS.md`, `CLAUDE.md`, `docs/TESTERS.md` (the new lines' names may appear as names; nothing explained), the design docs' deviations; `config/version` 0.5.0-rc2; `tools/test.sh` twice, `tools/check_boot.sh`, every smoke scenario; `tools/build.sh`, `tools/release.sh`; `git tag -f m5-candidate` moved to the commit (force-push the tag: `git push -f origin m5-candidate`).

## Deviations

(One bullet per departure from this plan or the design, "Task N:" first, as they happen.)

- Task 1: Main's `_fade_to`, `FADE_TIME`, and its `fade` reference went with the room transition (nothing called them); the `Fade` CanvasLayer stays in `main.tscn` for Task 5, which adds back what the fall needs.
- Task 1: the arena's floor-pattern RNG no longer keys on the round (`hash([seed, "arena_floor"])`): one arena is built per run, at round 0, so the round in the key was dead. The plan said `arena.gd` changes nothing beyond `door_sides`.
- Task 1: `EmperorBox` draws what `Door` drew, the closed leaf between the left and right frame pieces; `doors_frame_top`, named in the task's context, was never drawn by `Door` and is not drawn now.
- Task 1: the sound rows `room_enter` and `room_clear` and the wave tables `data/waves/room_N.tres` keep their names (they name files on disk and the plan said the tables are unchanged); only the handlers became `_on_round_started` and `_on_round_cleared`. The summary's title "Floor cleared" stays until Task 5 replaces the summary.
- Task 1: `test_summary_scene`'s death-during-the-fade test went with the fade; a death in the gap is `test_series_scene.test_a_death_during_the_gap_does_not_start_the_next_round` (it asserts the summary shows); the summary-over-fade layering assertion is the pre-existing one in `tests/test_summary_scene.gd`'s death test. `test_wave_defs` lost its room and floor cases; the shipped totals are pinned in `test_series_def`.
- Task 1: `CLAUDE.md`'s physics-layer, lifetime-rule, geometry, and test-helper lines were updated with the smoke line, since they named the door trigger, `Door`, the entry seal, and `quiet_main_with_floor`.
- Task 1: a forked session rewrote `tests/test_series_def.gd` and `tests/test_series_scene.gd` mid-task; both were checked against the plan's cases and kept (they add the spawner's per-round stream and the death in the gap).
- Task 1: "a round with nothing to offer goes to the gap at once" is implemented (`_offer_upgrade` with empty offers) but has no test: the plan lists none and an empty pool needs every card maxed.
- Task 1 review: `RunState.round` is `round_index` (a bare `round` shadowed the built-in; the naming bullet above amended); the `ROUND_GAP` timer pauses with the game (`create_timer(ROUND_GAP, false, false, true)`), so a pause screen opened in the gap holds the next round, with a scene test; the empty-pool round now has its test (`test_a_round_with_nothing_to_offer_goes_to_the_gap_at_once`); `clear_and_pick` asserts the picker opened; the smoke tool's margin is `TIMER_MARGIN` (0.1 s, the suites'); `EmperorBox` computes the gap once; `SeriesDef` explains its 8x6 minimums; `Spawner._ready`'s seeding is explained; `run_won`'s doc says it arrives inside the `round_cleared` physics callback; stale room and fade words fixed in `build_screen.gd`, `title.gd`, `test_title_scene.gd`, `CLAUDE.md` (feel constants, data list, real-time list), the plan's "Read this first", and `docs/ASSETS.md` (the door files kept without a row); the fade-test bullet above reworded (the layering assertion is `test_summary_scene`'s).

- Task 2: `FavourRules` also holds the granter names and the card counts (`GRANTER_CROWD`, `GRANTER_EMPEROR`, `granter(band)`, `offer_count(band)`), so Main only reads them; the plan put the names in Main. The "perfect ends below Roar" rule lives in `Favour._on_round_cleared`, after the clean-round bonus, not in Main: Favour is a child of Main, so its `round_cleared` handler is connected first and runs first, and Main reads the band with the bonus in (both files say so).
- Task 2: the crowd's sounds play on the UI pool (`Audio.play_ui`): the picker pauses the tree 0.8 s after the clear and a game-pool sound would freeze under it (the roar is 4 s). `round_ended` is emitted for the last round too, before `_win`, so the verdict sounds on the boss round's end as well.
- Task 2: four cards at 1280 wide leave no gap: `UpgradeMenu.card_gap(count, view_width)` shrinks `CARD_GAP` (40) to 0 and the frames touch edge to edge, the cards keeping `CARD_SIZE` as the plan asked (a pure test pins three cards at 40). Whether the row should shrink the cards instead is a playtest question.
- Task 2: the meter is the beige `PANEL` nine-patch at 3x (the sheet has no bar region; `framed_panel`'s insets are for the 4x frame) with a dark trough and the fill inset 6 px, at (16, 78) under the dash pips; the fill moves at once, no tween, since the drain moves it every tick.
- Task 2: "cowardice" is not an `ACTS` row: it is a rate (`COWARDICE_PER_SECOND` times delta, `FavourRules.cowardice`), and `favour_changed` names it "cowardice". Daring counts from the dash's end (its start plus `DashRules.DURATION`) and is scored on every kill inside the window after one dash through danger (the dash is not spent by the first kill).
- Task 2: the crowd-sound test sets favour to 40, not the plan's 60: a clean round adds 15 and 60 would end in Roar. The dash cases emit `player_dashed` by hand (the detector reads the bus; a real dash would shove the placed chaser). The cowardice test asserts at 3.9 s and 5.0 s (235 and 300 ticks), not at the 4.0 s edge, where the float sum of 1/60 decides the tick.
- Task 2: `CLAUDE.md` got a Favour convention line (the acts table, one detector per act, `RunState.elapsed` as its clock).

- Task 2 review: `Favour` forgets the last kill, the last daring dash, and the engagement clock on `run_started` (a restart puts `elapsed` back to 0 and the next run's first kill chained with a phantom), with a scene test; its handlers are one `_handlers()` table connected in `_ready` and disconnected in `_exit_tree` (audio.gd's pattern); the cowardice tick checks the grace before walking the enemies group and `_any_harmful` returns early; the dash segment's comment says it is the nominal path, an estimate; `FavourRules.COWARDICE_ACT` names the drain; `hits_this_round` is cleared by Favour at `round_started` (Main's line dropped: favour state has one owner); `Audio.CROWD_SOUNDS` is a Dictionary keyed by `FavourRules.BOO..ROAR`; `Array[Vector2]` for the dash positions, typed arrays and no unused binding in the favour suite; a scene test drives the real dash through a chaser and scores daring; `FAVOUR_BAR_POSITION` says it sits 8 px under the Dashes row. Also: the two projectile-clearing tests in `test_series_scene` wait one more frame so the freed shots flush before gdUnit's orphan snapshot (an intermittent 3-orphan warning, the tests' own race).

- Task 3: `coin_anim` was not in `data/atlas.json`: the sheet lays its 6 px frames 8 px apart and `gen_atlas.py` dropped the animation as non-contiguous. The generator now records a `stride` when the frames are evenly spaced wider than the width (the coin is the only such sprite; `zombie_anim`, out of order, stays dropped) and `SpriteAtlas.region` steps by it; the regenerated atlas gains only `coin_anim`, pinned in `test_sprite_atlas`.
- Task 3: `PileRules.pile_count` is one pile per `COINS_PER_PILE` (8) coins rounded up, held to 4..8, and never more piles than coins (a pile of nothing would be a coin that pays nothing): 4 piles up to 32 coins, 8 from 57. `throw_piles` with a total of 0 does nothing and emits no `coins_thrown`.
- Task 3: the boss's coins never touch the counter or the tally (the design: "the boss's coins are always thrown"); Main's `_on_enemy_died` throws them deferred when the enemy is in the `boss` group and pays every other kill through `_pay` (the counter, `coins_changed`, one `CoinFlight` from the corpse). The plan's behaviour paragraph reads as if every kill's coins fly.
- Task 3: the piles' stream is drawn once per round in `_enter_round` (`Main._pile_rng = RunState.stream("piles:<round>")`) and every throw of the round draws from it, so a Roar on the boss round and the boss's own throw land on different spots; a fresh stream per throw (the plan's wording) would give both the same offsets.
- Task 3: the counter sits at the right under the build strip (`COIN_COUNTER_TOP` 112, inset 16), not in the top-right corner, which the Info label holds; the coin sits to the right of the number so it stays put as the number widens and the flights land on it (`Hud.counter_position`). `hud.gd` got `class_name Hud` so Main calls `fly_coin` typed; `EmperorBox` keeps its `gap` and answers `centre()` for the Cheer flight's start; `CoinPile.land(at)` is public (the toss's end, and the tests' way to put a pile on the floor).
- Task 3: `coin_get` plays on the game pool: a flight lands 0.45 s after the kill, before the picker's pause at 0.8 s; the flight ignores the time scale so a kill freeze does not hold it, the toss does not (a pile hanging in a freeze is fine). `RunState.round_tally` is cleared by Main in `_enter_round` (it owns the round flow and is the tally's one reader).
- Task 3: the physics server pairs a body placed on a pile in the frame the pile is made only after a tick, so the collection tests give it one; the dash case sets the dash by hand (`dash_left` long, `dash_dir` zero, the layer and mask) because a real dash moves 5.5 px a tick and leaves the pile; an extra case pins that a pile landing under a standing player pays (a Roar throws around the player). The counter's text is asserted in `test_coins_scene`, not `test_hud_scene`. `CLAUDE.md`'s layer line names the pile as layer 6's user and the feel-constants list names the new files.

- Task 3 review: `RunState.add_coins(value)` is the one writer of the run's coins and the one emitter of `coins_changed` (Main's `_pay` and `CoinPile` call it; a run-state test pins the totals); `gen_atlas.py` rejects a stride under the width (overlapping frames) and uneven spacing with a message naming which, the atlas regenerated unchanged, the tileset README says when `stride` is written; the counter's top and inset are read from `BuildStrip.offset_bottom` and `Info.offset_right` at build time, only `COIN_COUNTER_GAP` is a constant; the deferred throws go through `_throw_piles_in(target, at, total)`, guarded on the room like `_clear_projectiles`, with a test (a Roar cleared then `play()` in the same frame lands no piles in the new run: the boss path could not host that test because `Boss._kill_summons`, deferred on a boss freed with its room, trips `get_tree()` null, a pre-existing quirk left alone); `Room.global_bounds()` is what `throw_piles` and the seeded-spots test use; the flight tests assert the start (the corpse, the box's centre, through the viewport's canvas transform) and `Hud.counter_position` stays public; `player_of`/`hud_of` live in `SceneSuite` (the favour suite uses them too), `_throws`/`_pickups` are `Array[Array]`; the rules test's bounds come from `ArenaGrid.bounds(28, 15)` (a var: not a constant expression); `PileRules.spots` clamps the centre into the shrunk bounds first so a throw from the edge band keeps every spot within the radius (the near-wall case measures from the clamped centre); the `_pile_rng` comment names the harness-only staleness after `restart()`; a test pins that `round_tally` starts over at the next round of one run. The Roar-pickup scene test failed once in a suite run that also carried the boss quirk's runtime error, and passed in the next three full runs. Re-review: `Boss._kill_summons` returns when the boss is out of the tree (the deferred call after a harness's `play()` in the death's frame; no test, since under `push_error=true` the stray error would land in whichever test ran then, and the guard matches the `is_inside_tree` check two lines above it); the Roar test raced the picker (its waits left about 0.2 s before the pause, and a paused `Area2D` fires no `body_entered`), so it now plays the real sequence: the piles land, the picker opens over them, a pick closes it, and the pickup pays in the next round, one tick later than a plain placement because the first step after the unpause reports no overlap yet (probed), plus a frame for the paid pile to leave the tree.

- Task 4: `add_stat`'s assert cannot be a passing test (a failed `assert` is a script error under the runner, not a failure): the rule it checks is the pure `Save.addable(key, id)`, tested directly; `stat` and `set_stat` assert too.
- Task 4: `STAT_KEYS` is a Dictionary name to empty value (`{}` for a per-id counter, `0` for a counter, `0.0` for a time or a peak, `{}` for `best_run`), `PER_ID_KEYS` names the nested ones, `NOT_ADDABLE` the three that are not counters (`best_run`, `boss_time_best`, `favour_peak`); their setters are `set_best_run(record)` (true when it replaced, by rounds then kills, a tie keeps the held one), `set_boss_time(seconds)` (the min; 0.0 is none yet), `raise_stat(key, value)` (the max); `set_stat` overwrites a plain stat with a value of its own type. `FLAG_KEYS` is a Dictionary of defaults with `returned` a bool.
- Task 4: the file's layout: `[meta] version`, `[money] value`, `[training] <line>=rank`, `[flags] <flag>`, `[stats] <stat>` (a Dictionary for a per-id key), `[runs] log` (the Array of records). A value of the wrong shape (a per-id key holding a bare number, a counter holding a table, a log that is not an Array) loads at its default, with a test.
- Task 4: a per-id stat never has an empty key: `Profile` files a source it cannot name (a bare `hurt` in a test, a stub enemy with no def) under `Save.UNKNOWN_ID` ("unknown"); the signals carry "" as the design says. `Player._id_of(def)` reads the def's id ("" when it has none) and `last_attacker_id` remembers the last hit's for `player_died`.
- Task 4: `SceneSuite` gained `before_test` (the plan's file list named `after_test` only): it points `Profile.path` at `PROFILE_SCRATCH` and resets, so no test ever counts on the player's file; `after_test` removes the scratch and resets again. `test_coins_scene` and `test_shield_scene` call `super()` in their `before_test`.
- Task 4: `dashes_through_danger` counts "daring" acts, which since Task 2 are scored per kill inside the window after one dash through danger, so it is a count of daring kills, not of dashes (said in `profile.gd`). `time_played` sums `_process` delta, which `Engine.time_scale` slows under a kill freeze, like `RunState.elapsed`; the autoload pauses with the tree, so a menu adds nothing.
- Task 4: beyond the plan's cases, `test_profile_scene` pins the boss's bolt id (`Boss._fire_bolt`), the boss kill under `boss_kills`, the unknown-id rule, `player_died`'s attacker, the bus-only stats (rounds, bands, clean rounds, dashes, daring, the peak, piles), `time_played` under a pause, and `reset()` reloading the committed file; `test_favour_rules` pins `band_name`.

- Task 4 review: a save file the game cannot use (unparsable, or `version` above `Save.VERSION`) is copied to `<path>.bak` (`Save.BACKUP_SUFFIX`, over an older one) before the defaults load, so the first `commit()` no longer destroys it; `Save` stays pure and records what it did in `backup_note`, and `Profile.reload()` pushes the warning (a `push_warning` inside `load_from` failed the tests: gdUnit reports warnings like errors). Godot's `ConfigFile` parser prints an ERROR on a corrupt file and has no silent parse (probed: `core/io/config_file.cpp` `_parse`), so the corrupt-file test mutes `Engine.print_error_messages` around its one load, and `check_boot` on a machine whose real save is corrupt would report that line. `addable(key, id, amount)` also checks the amount's type (an int for an int counter, an int or a float for a float stat) and `add_stat` sums in the counter's type, so an int never drifts to a float the next load would drop; `commit()` warns on a failed write like the build screen does for the settings; `stat()` reads a per-id value as an int; the run log is capped on load; `set_best_run` breaks a rounds-and-kills tie by the faster time; the dead pause guard in `Profile._process` went (the autoload pauses with the tree; the pause test still proves it); the stat-table test pins the design's count (24) and that `PER_ID_KEYS` and `NOT_ADDABLE` are subsets, so a new key is a row plus a list entry; `tools/smoke.gd` points `Profile.path` at `user://smoke_profile.cfg` so no scenario can write the player's save once the verdict commits (`smoke.sh idle` ok). The shared `id_of`/`remove_user_file` helpers the review named as optional were not done. Re-review: the smoke tool removes an earlier run's scratch before the reset reads it; the stat-table test names the 24 stats again (a count let a misspelt key pass) beside the subset checks; `SceneSuite.after_test` removes the scratch's `.bak` too.

- Task 5 (coordinator decision): the piles still on the floor at a thumb up (a Roar's tally on the last round, the boss's 60) are swept into the run's coins before the banking (`Main._sweep_piles`: each pile's value through `RunState.add_coins`, a `CoinFlight` from it to the counter, the pile freed), at the start of `_verdict` before the thumb; on a thumb down they are lost with the rest. The design's "piles stay until picked up or the run ends" reads "or are swept at a thumbs up"; `test_a_win_reaches_the_gate_with_the_boss_piles_banked` and `test_a_win_sweeps_the_piles_on_the_floor_into_the_bank` pin it.
- Task 5 (coordinator decision): `tools/reload_probe.gd` points `Profile.path` at `user://probe_profile.cfg` before anything (the file removed first, then `reset()`, as the smoke tool does; removed again at the end), since the yield path it drives now commits; it also walks the new path (after the second `play()`, a `hurt(100, ..., "chaser")`, the verdict scene's real time plus the fade, the gate screen asserted visible, then `quit_to_title()`). The probe stays duck-typed throughout (`root.get_node("Profile")`, `get("player")`, the constants through `get_script_constant_map()`): naming `Main` or an autoload in a `-s` script compiles the game before the autoloads exist and every script fails on `Events`.
- Task 5 (coordinator decision): `FADE_TIME` (0.15) and `_fade_to(alpha)` are back in Main (a tween on `Fade/Black`'s alpha, ignoring the time scale) for the verdict's fade to black; `restart()` puts the alpha back to 0 (the harness has no reload to do it), so a pass through the gate lands on a lit arena.
- Task 5: `WIN_SUMMARY_DELAY` is `WIN_HOLD` (1.0, the beat on the boss's corpse after its own hold before `_verdict(true)`), `DEATH_SUMMARY_DELAY` is `VERDICT_HOLD`. The `player_die` row plays on `player_fell` still; `crowd_hush` (on `player_fell`) and `verdict_up`/`verdict_down` (on `verdict_given`) play on the UI pool like the hush's neighbour `player_die`: the gate screen pauses the tree a couple of seconds later and a game-pool sound would freeze under it. The `win` and `lose` rows went from `data/audio.json` and the audio test's list (the verdict's sounds are the run's sting now; the files stay in `assets/sfx` like the door's). The gate's sound is `menu_closed("gate")`, which Main emits in `_pass_gate` (Enter or a click): an Esc to the title or an R from the screen passes no gate and plays none; the screen's open emits `menu_opened("gate")`, mapped to nothing.
- Task 5: the gate screen pauses the tree like the picker (`show_gate` sets `paused` after `Juice.reset()`, `close()` unpauses), since the arena under the black is done and a live shooter's bolts would sound under the screen; Main stops the game pool as it opens so nothing frozen resumes next to the next run. R on the screen is handled there (`restart_pressed`, like the picker's), because Main is paused; `ui_accept` and a left click emit `continue_requested`, Esc `quit_requested`. The screen's labels are built in code from `UiTheme` (Alagard at 64 for the gate, Pixel Operator at 32 for the blocks: the fonts land on the 16 px grid), the two blocks side by side in an `HBoxContainer` (`Blocks/Run`, `Blocks/AllTime`, split from `body()`'s blank line), the portrait an `AnimatedSprite2D` at 4x in a 160 px `Portrait` box under them, hidden when no hit was ever taken or the id has no `data/enemies/<id>.tres`.
- Task 5: the run's hit count is `RunState.hits_taken` (from `player_hit`, reset by `start_run`), what the record's `hits` and `VerdictRules.decide` read; the record also carries `rounds_total` (the screen shows "Rounds n/8"). A yield counts `runs` as well as `falls` (the plan named only `falls`): `runs` stays `wins + falls` and every logged record is a counted run. `perfect_runs` is both a flag and a stat since Task 4; a perfect win bumps both. A thumb down after a win (only with `verso`) files `deaths_by` under `Save.UNKNOWN_ID`: no attacker. `Save.total(key)` sums a per-id stat (the all-time kills, hits taken, shots fired on the screen). The boss's fight time is `boss_spawned` to the boss's `enemy_died` in Main, kept only on a win.
- Task 5: `ThumbSign` is a child of the Room (`scenes/room.tscn`, placed by `Room._ready` through `place_over(emperor_box)`), not of Main: the Room is rebuilt per run and the box is its. It sits with its top flush with the box's top edge (the arena's, and the screen's), centred on the box, 48 px square at `SCALE` 3 over the 32 px box: the plan's "a tile above the box" is off the top of the screen. The fist and the thumb are `Rect2i` constants mirrored for a thumb down, with a one-pixel edge pass. `restart()` also resets `_ended` (through `_forget_run`, which `play()` shares) so a harness's new run can end again; the yield guard is `not _ended and not title.is_open()`.
- Task 5: the smoke scenario `fall` captures `reports/smoke_fall_verdict.png` at the thumb (the sign over the box, the flat gladiator; the first `SMOKE_IMAGE` line, which smoke.sh's luminance floor reads) before the end capture `smoke_fall.png`, the gate screen over the black, whose mean luminance (0.015) is under the floor by design. `test_series_scene`'s death-in-the-gap test asserts the thumb instead of the summary (the gate screen lands 2.35 s after the fall, past the test's wait).
- Task 5: `test_fall_scene`'s win case waits for the boss to be active (`boss_spawned`, the fade-in's end) and sets the favour to `FavourRules.MAX` before the kill: killed inside its fade-in there is no spawn time for `boss_time_best`, and a round ending at Cheer is no perfect run.

- Task 5 review: the verdict scene cannot be skipped: `restart()` and `quit_to_title()` return while `_verdict_pending()` (`_ended` and the gate screen not open), so R, Restart, and Quit to title between the fall (or the last clear) and the gate screen do nothing and the run is recorded once, at the verdict (the screen's own R, Esc, and pass arrive with it open); this also closes a harness leak where a `restart()` during `_fade_to` zeroed the fade under a tween still running. `Save.bump_flag(key)` (asserting `Save.bumpable`: a flag that is a count, not `returned`) is the one writer of the flag counts; `_bank` and `_yield` end in `_close_run(outcome, verdict, kept)` (runs bumped, the record logged, `commit()`, `run_ended` emitted in that one place, so it now precedes `verdict_given`). `GateScreen.blocks(run, save) -> PackedStringArray` is the text unit and `body()` its join; a click is accepted only from the frame after `show_gate` (`_just_opened`, cleared by `_process`: a frame's input runs before its `_process`), with a test that clicks in the opening frame and the next. `_on_boss_spawned` keeps the first boss's time. The fall suite reads the screen's public labels and names the win-hold wait. The flake the reviewer saw (4 failures in one `test_coins_scene` case, green alone) did not reproduce in three further full runs here; the case was not named in the log kept, and the reviewer's run overlapped this session's own suite and smoke runs on the same machine (the same `.godot` import cache and `user://` scratch files). By reasoning, every wait in that suite that sat on a clock (a flight's `FLIGHT_TIME`, a toss's `TOSS_TIME`, the picker's `PICKER_DELAY`, the `ticks(3)` for the pile under the unpaused player) now waits on its event through `_wait_until(condition, what)` (a per-physics-frame poll with a frame cap that asserts the condition held): the flights gone, every pile's `monitoring` on (its `land()`), the picker open, `pile_collected`, the coins paid, the paid pile gone. The Cheer case also waits out the kill freeze explicitly before the clear. Two leaks the fix pass's own full runs caught: `test_player_damage_scene`'s lethal case pressed R 1.3 s after the fall and expected a restart, which the new rule refuses (it now waits for the gate screen and presses R there); and the click test fed a left press with no release, which held the `shoot` action (the same button) for every later suite, so `_click()` feeds the press and its release.

- Task 6: `TrainingRules.apply(save)` returns `{max_hp, dash_charges, favour}` (the plan's `apply(save, build)`): `Build.starting(save)` sets `base_max_hp` and `base_dash_charges` from it and the player cards fold over those (coordinator note 6), and `RunState.start_run` sets the favour from it; the purchase itself is `TrainingRules.buy(save, line)` (money out, the rank up, `coins_spent`; pure on the Save), which the panel calls before `training_bought` and `Profile.commit()`. `rank`, `max_rank`, `capped`, and `next_price` are the rest of the API (`price(line, rank)` is the rank-th rank's price, 1-based).
- Task 6: the stations are made in code by `Grounds._make_stations` from `ArenaGrid` (`Station.setup(id, top_left, sprites, rect)`), not placed in `grounds.tscn`, which holds only `Arena`, `Stations`, and `Projectiles` (the player can shoot in the grounds; its shots need a parent that goes with the stage). The rack's and the gate's areas are the floor row under their art (the ring is solid; the player stands below the wall), the post's the crate grown by `AREA_MARGIN`; `Station.stand_position()` is the area's centre in world pixels, what the tests and the smoke tool walk to. The rack's weapons are `weapon_knight_sword`, `weapon_axe`, `weapon_bow`.
- Task 6: `RunState.start_run` reads `Profile.save` directly (every autoload is a named global before any `_ready` runs, so the boot's `start_run` in `RunState._ready` sees Profile's default Save: the bases), not through a `get_node_or_null` guard.
- Task 6: the run's music moved from `run_started` to `round_started(0, _)` (the plan's row): `RunState.start_run` is also what a harness restart and `SceneSuite.after_test` call, and neither is a run's start in the grounds; the title's `menu_opened("title")` keeps playing it. `Player.revive()` listens to `run_started` instead: a fresh run's body is whole with the build's maxes (the training's bases) filled, emitting nothing; the HUD re-reads the player's hearts and pips in its own `run_started` handler (a later child, so connected later). `enter_grounds` and `enter_arena` call `revive()` too (the fallen gladiator walks in the grounds).
- Task 6: `_pass_gate` is no restart (the plan said "`enter_grounds()` and `flags.returned`"; the code before it called `restart()`): the gate screen closes, `_forget_run`, `_run_serial` moves on, a fade to black (a no-op after the verdict's, cover for a harness that opened the screen bare), `enter_grounds()`, `Save.set_flag("returned", true)`, `commit()`, the fade back; `restart_requested` is not emitted and nothing is yielded (the run closed at the verdict). `test_fall_scene`'s two gate-pass tests now assert the grounds, not a restart.
- Task 6: `restart()` yields only with a room up (`room != null and not _ended and not title.is_open()`); in the game the reload rebuilds the arena, and in a harness (no reload) a restart from the grounds calls `enter_arena()` and `_enter_round(0)` so both land in the arena. Quit to title from the grounds therefore shows the title over the boot's arena in both, not over the grounds as coordinator note 4 read (the reload rebuilds Main either way).
- Task 6: the training rows take the click through `gui_input` (a disabled `Button` still receives it, and a refused click has its own sound), not `pressed`; `TrainingPanel.click(line)` is the handler the tests call. The coin icon on the rows and the renown line is the HUD counter's three lines repeated (a static `_coin_rect`; the HUD's is not a helper). The armoury's lit slot shows `Build.STARTING_WEAPON`'s icon, not the last run's build (every run starts with the handgun). Neither panel emits `menu_opened`.
- Task 6: `SceneSuite.wait_until(condition, what, max_frames)` is the coins suite's `_wait_until` moved up (the coins suite calls it); a placement pairs with a station two or three ticks later (probed), so the grounds suite waits on `has_overlapping_bodies` and on `main.grounds == null` for the gate. A mouse click on a Control in the headless runner must be given in window pixels (`get_viewport().get_final_transform()`: the window is 64x64, scaled 0.05), which the row-click test does. The suites whose last line swaps the stage await a frame in `after_test` so gdUnit's orphan count does not see the queued frees.
- Task 6: the Task 5 re-review leftover (the Roar case in `test_coins_scene`): the flake was a pile landing under the standing player (the spots are drawn in a 64 px disc around it and pay on contact, about one seed in eight; probed seeds 10 and 22 do it), not the toss's timing; the test now seeds the run (17, the closest pile 40 px off), waits on every pile's landing as the note asked, and asserts every pile's clearance from the body.
- Task 6: `docs/TESTERS.md` was left for Task 7 (coordinator note 1).

- Task 6 review: R and the pause screen's Restart do nothing in the grounds (`restart()` returns while `grounds != null`; the gate is the only way into the arena, and the harness path that put the arena back went with it); Quit to title from the grounds yields nothing and, in a harness, shows the title over the grounds (in the game the reload rebuilds the boot's arena under it), the title's seed spent and `_run_serial` moved so a gate fade under way starts nothing. `time_in_grounds` counts in `Profile._process` from `grounds_entered` to `run_started`; `shots_fired` and `dashes` count only while a run is live (`_in_run`: set on `run_started`, cleared on `run_ended` and `grounds_entered`, true at boot since RunState starts its run before Profile listens, and `reset()` puts both flags back). `Player.can_fire` is off in the grounds (set by `enter_grounds`/`enter_arena`), and the grounds' `Projectiles` node went. The stage swap is one `_mount_stage(stage)` (the old stage freed, the panels closed, the new at index 0, the player at `entry_position()` with the stage's `Projectiles` read once it is in the tree, since an `@onready` container is null before that, and the camera on `full_rect()`), with `_start_first_round()` for the boot and every run's start; `enter_arena` no longer revives (the run's start did, or the grounds had). `_forget_run` clears `_pending_seed`/`_pending_cheats` (`_start_run` reads its arguments first), with a flow test through a fall. Minors: the row handler's local is `press`; the real-mouse test clicks the greyed row too; `SpriteAtlas.rect(name, scale)` serves the HUD's counter and the rows; `SPEAR_LEAN`, `RACK_GAP`, `ROW_SEPARATION` named, `Station.new()`, the station docstring, the armoury's box from the idle frame's size (`GLADIATOR_AIR` around it); `plays`, `fall_to_the_gate`, `pass_the_gate`, `click_control` and the frame flush in `SceneSuite` (every `after_test` override now `await super()`: a bare `super()` on the awaiting base ran the rest of it under the next test). The gate screen's opening-frame click test keeps its own synchronous `_click()` (the screen reads any click in `_input`; the pixels do not matter) and opens the screen from a timer's continuation as Main does, since a body resumed by `process_frame` runs before the frame's `_process`, where `_just_opened` would already be cleared under a buffered click.

- Task 7: `docs/STATUS.md` keeps the Milestone 2 to 4 history bullets, which name rooms, doors, and the summary as what those milestones built, with a note on the M2 bullet that M5 replaced them; the live sections (the top, "How to run and verify", the tuning list, the milestone state, the next steps, the handoff) name none. `CLAUDE.md` gained a "Coins" convention line and the pools rule on the audio line (the sounds that must outlive the pause that follows them play on the UI pool) beyond the task's list. The tester file's questions name the meter, the coins, the thumb, the two gate names, and the three places without saying what any of them does; its "For testers only" note is the one place a mechanic is named (the three cheats). The grounds suite's shot counter is a suite handler connected in `before_test` and disconnected in `after_test`, and the shot after the gate is waited on through `wait_until` (the two tidy-ups from the Task 6 re-review).

- Final review of the milestone (2026-09-25, after the release): `tests/test_run_state.gd` ran first in the runner on the live profile (before any `SceneSuite.before_test`) and would fail once the real `save.cfg` held a training rank; it now uses the scratch path (proved against a real file: red before, green after). The design's first-run rule corrected to `flags.returned`; the profile's callers list gained the gate-screen pass; the tester file no longer says "walk into things" or asks what was bought. Left for the playtest fix pass: `_run_serial` bumped in `_forget_run` (a harness `play()` during the verdict scene could bank on the new run); the Switch card's subtitle explains its mechanic (an M3 text, a row for M6); `CLAUDE.md`'s real-time list omits `Hud.BOSS_BAR_TWEEN`, `Hud.VIGNETTE_TIME`, `Fx.AFTERIMAGE_LIFE`, and `test_juice_scene` waits on bare literals; the record's `training` is logged at the run's end (equal to the start today).
- Task 8 (note 7): a win keeps the `VERDICT_SHOW` beat after the sweep (the sweep's flights land in it) before the fade; the plan listed the sweep, the bank, the fade. The `verso` cheat turns only a fall's thumb now: a win is always up (no `VerdictRules.decide` call), so `_bank`'s unknown-id death (a win turned down) is reachable only from a fall whose attacker id is empty. `Audio._on_run_won` plays the fanfare (`verdict_up`) at the win itself (the `run_won` emit in `_win`, before `WIN_HOLD`), not at the ceremony. The record's `verdict` field stays "up" for a win.
- Task 8 (note 8): the money line is right-aligned over the prices' column (the number then the coin, like a row's price), `MONEY_HEIGHT` 56 over the rows; `money_text()` reads it.
- Task 8 (note 10): the row grew a second line: `Box` (the icon, the pips, the price) over a `Line` label at the row's left (`ROW_TOP_HEIGHT` 56 over `LINE_HEIGHT` 32; the panel 400 x 408); `TrainingRules.text(line)` reads the table's new `text` field. The three texts are placeholders until Task 11's lines.
- Task 8 (note 15): `EnemyDef` had no `display_name` (only `BossDef`): added, exported, validated non-empty (the enemy def suite's fresh defs name themselves through `_fresh()`), with Imp, Shield imp, Shaman in the data. `GateScreen.enemy_def(id)` is the one loader for the portrait and the line (an `EnemyDef` or the `BossDef`, read duck-typed); the label is `PortraitLine` under the portrait box, hidden when the line is "".
- Task 8 (note 16): the Raven sheet has no crowd or laurel; the favour icon is (129, 13), a silver mask with an open mouth (the crowd's face), added as `favour` (`data/icons.json` regenerated: that entry only). The two rows are laid out in code under the hearts, each centred on its 48 px icon (`ROW_ICON_SCALE` 3, `ROW_ICON_GAP`, `ROW_STACK_GAP`): the scene's `Dashes` offsets are overridden and `FAVOUR_BAR_POSITION` went. `tools/icon_sheet.gd` did not run (it read `IconAtlas.SHEETS`, removed earlier); it renders through `IconAtlas.texture` now.
- Task 8 (note 6): the sign's placement is unchanged (its top flush with the arena's top edge, centred on the box): the coordinator's "bottom on the arena's top edge" puts the whole sign off the screen (the Task 5 finding). The fist is 16 by 12 with the bottom corners rounded two deep (the top-right carries the last knuckle), the knuckles two pixels tall with open notches, the shade laid inside the silhouette (the edge pass darkens every rim pixel, so a shaded rim would vanish). For the record: the Raven sheet carries a drawn thumb up (54, 15) and thumb down (55, 15), an alternative to the code-drawn sign if wanted.
- Task 8 (note 13): each card's frame and column sit on a `Body` control inside the Button (the row places the button; the face is what slides; the card tests reach labels recursively, so nothing else changed). The reveal runs only from a closed menu: a re-open while open (a refund round) shows all four at once. `_open_serial` (bumped by open and close) lets a reveal timer from an earlier open do nothing; the face is parked off the view's right edge as the card is added, then set to its measured offset after the layout frame (without it the card drew one frame in place before sliding). `choose(index)` refuses a slot whose card is not built. The four-card capture used a temporary scenario in `tools/smoke.gd` (reverted with `git checkout`, never committed) rather than a scene file; the slide was measured in the same way (289 px to 0 over the 0.25 s).
- Task 8 (note 1): the pile finds the player once at `land()` (`get_first_node_in_group("player")`); Main passes nothing. A Roar's piles land within `PileRules.PILE_RADIUS` (64), inside the reach, so the pull pays them before the picker opens (`test_a_roar_throws_the_tally_around_the_player_and_the_pull_pays_it_before_the_picker` replaces the survive-the-picker test); the win's sweep test places its pile at 180 px so the sweep, not the pull, pays it. A pile out of reach resets its speed, so a player who steps away and back is met at `PULL_SPEED` again.
- Task 8 (note 2): the cap is a `SceneTreeTimer` with process_always off (it pauses with the tree and ignores the time scale), polled each physics frame, not a tick count: `physics_frame` fires under a pause, so counted ticks would run on under the pause screen. The gap's guards are `_gap_holds(target, run)`, shared by the wait and the start.
- Task 8 review: (A) the thumb is the Raven sheet's drawn hands (`thumb_up` (54, 15), `thumb_down` (55, 15) in `tools/gen_icons.py`, `data/icons.json` regenerated) on `ThumbSign`'s sprite at `SCALE` 3 (48 px), nearest-filtered, placed as before; the code-drawn fist and its constants went (`SIZE` is `IconAtlas.SIZE`). (B) the favour icon is a 16x16 two-heads PLACEHOLDER drawn in code (`Hud.crowd_placeholder`, `CROWD_FILL`/`CROWD_EDGE`/`CROWD_HEADS`): the mask read as a character; the `favour` entry left the icons table. (C) `PULL_RADIUS` (96) lives in `PileRules` (the rules table; `PULL_SPEED`/`PULL_ACCEL` stay on the pile) and `RunState.pull_radius` reads it; `PileRules.spots(center, count, bounds, rng)` lost its radius parameter and places every throw (a Roar's tally around the player, the boss's coins where it fell) in a ring from `RING_MIN` (`PULL_RADIUS + RING_GAP` 16 = 112) to `RING_MAX` (160), clamped inside the floor: each pile takes the first of `RETRIES` draws that is clear of the others and still `RING_MIN` away after the clamp, else the clear draw furthest out (a wall in the way), so a centre near a wall lands on the open side. `PILE_RADIUS` went. The arena floor is 176 px tall inside the edge band, so the ring's top and bottom are off it and the spots fall left and right of the player. The Roar scene test has its old shape back (the piles survive the picker and pay in the next round); its last asserts tolerate a neighbour pulled in once the player walks to the first pile. `_clamp` lands spots on the shrunk rect's far edges, which `Rect2.has_point` excludes, so the rules suite checks a closed rect. (1) the dash row sits `ROW_STACK_GAP` under the hearts' real height (`Hud.heart_size()`: the 13x12 sprite at 3x, 36 px, not an icon's 48), the HUD test exact. (3) `_on_run_won`'s docstring back on it. (4) the card's face node is `Face`; `hold_last: bool`; the docstrings say the fourth card can be taken while it slides. (5) `MONEY_HEIGHT := ROW_TOP_HEIGHT`, `LINE_HEIGHT := float(LINE_FONT_SIZE)`, `PANEL_SIZE` from `TrainingRules.LINES.size()` (a `static var`: the table's size is no constant expression). (6) `show_gate` resolves `deadliest` and `enemy_def` once and passes the id to `portrait_line(save, id)` and the def to `_show_portrait`; `enemy_def`'s doc says why it returns a `Resource` (`BossDef` does not extend `EnemyDef`). (7) `hud.tscn`'s `Dashes` offsets zeroed; `_ready` notes `_build_row_icons` runs after `_build_favour_bar`. (8) `_bank(outcome, up)`. (9) the pile-wait test waits on `started.size() == 1`. (10) the series suite's round watcher and the menu suite's reveal watcher are disconnected in `after_test`. (11) note 4's Done cell and the progress line name the one SHA.
- Task 9: a scoring act is defined by the table, not a list: `FavourRules.is_scoring(act)` is true for any act whose value is positive (every act but the hit), and `Favour._score` restarts the decay's clock (`last_scoring_time`, replacing `last_engagement`) on one; the `enemy_hit` detector went (nothing read it once a hit stopped holding the drain off). The hit's scene test presets the meter at 60 (from 20 a hit only shows the clamp at 0); the decay test asserts at 2.9 s and 4.0 s (174 and 240 ticks), not the 3.0 s edge, with a hit on the enemy at 1.5 s that extends nothing and a kill (a second chaser keeps the round live) that holds the decay off for a fresh grace. Renown adds to `START`, so the training, run-state, and grounds suites re-pinned one rank at 30 and three at 50. The HUD suite's meter test asserted the start in Quiet; it now reads the band from `FavourRules.band(START)` (Boo from 20). The design's acts table still shows the M5-as-designed numbers; Task 12 records the design docs' deviations. The review's estimate showed the meter still maxing over a hit-free run (it carries across rounds and any kill resets the decay), and the user answered with two rules, landed in `TASK9B_SHA`: (1) kills cap at the Roar edge: `FavourRules.CAPPED_ACTS` (kill, chain) and `KILL_CAP` (75, `BAND_EDGES[ROAR - 1]`; a test pins the tie), `apply` stopping a capped act at the cap and adding nothing above it (a capped kill above the cap is still a scoring act: it holds the decay off); daring and the clean round push past. (2) The crowd cools between rounds: the decay needs no harmful enemy, only a live run, `Favour._run_live`, true from `run_started` or `round_started`, false on `player_fell`, `run_ended`, and `grounds_entered` (the plan named the first two; the grounds test's Main boots into a run before entering the grounds, and the grounds are never a live run); `_any_harmful` went. It runs through the gap, the coin wait, and a wave's spawn-in; the picker and the pause screen stop it by pausing the tree. The gap test measures half a second of the one-second `ROUND_GAP` (0.75 lost) after jumping `elapsed` past the grace, since a full second would cross the next round's start, which resets the clock; the pause screen is opened in the gap and holds the meter.

## Progress

- 2026-09-25: plan written from the approved M5 design; the sounds it names are in place. Next: Task 1.
- 2026-09-25: Task 1 landed in `ea69a55`: `SeriesDef`/`RoundDef` and `data/series/tier_1.tres`, one `Room` for the run with solid walls and the `EmperorBox`, `_enter_round` and the `ROUND_GAP`, `round_started`/`round_cleared`, `RunState.round*`, "Round n/8", the `round` smoke scenario; `Door`, `FloorDef`, `RoomDef`, `FloorRules`, `Arena.seal`, and the door sounds' rows gone. 428 tests green twice, `check_boot` ok, `smoke.sh round|boss|pick` ok. Next: Task 2.
- 2026-09-25: Task 1 review pass landed in `c3ea83f` (see the "Task 1 review" deviation). 430 tests green twice, `check_boot` ok, `smoke.sh round` ok. Next: Task 2.
- 2026-09-25: Task 2 landed in `40511d8`: `FavourRules` and the `Favour` node (kill, chain, daring, clean_round, hit, the cowardice drain), `RunState.favour`/`perfect`/`hits_this_round`, `round_ended(band)` and `favour_changed`, the crowd's four sounds, the granter over the cards and the fourth card with `pick_4` on a Roar, the meter under the dash pips. 455 tests green twice, then 456 once with the card-gap test, `check_boot` ok, `smoke.sh pick` ok (the granter label read in `reports/smoke_pick_menu.png`, the four-card row and the meter in throwaway captures). Next: Task 3 (coins), after the Task 2 reviews.
- 2026-09-25: Task 2 review pass landed in `700d14d` (see the "Task 2 review" deviation). 458 tests green twice, `check_boot` ok. Next: Task 3.
- 2026-09-25: Task 3 landed in `5cfd2f8`: `EnemyDef.coins`/`BossDef.coins` and the shipped values, `RunState.coins`/`round_tally`, `coins_changed`/`coins_thrown`/`pile_collected`/`coin_landed`, `PileRules`, `CoinPile` (`scenes/coin_pile.tscn`, layer 32 masking 65, the toss), `CoinFlight` and the counter at the right of the HUD, Main's kill pay, the Cheer bonus and the Roar throw, the boss's throw, the three coin sounds; `coin_anim` reached the atlas through a frame stride. 477 tests green twice, `check_boot` ok, `smoke.sh kill` ok (the counter read "1" in `reports/smoke_kill.png`). Next: Task 4 (the profile and the save), after the Task 3 reviews.
- 2026-09-25: Task 3 review pass landed in `cd354b8` (see the "Task 3 review" deviation). 481 tests green twice, `check_boot` ok. Next: Task 4.
- 2026-09-25: Task 4 landed in `de33fdf`: `Save` (money, training, flags, `STAT_KEYS`, the run log, `user://save.cfg`), the `Profile` autoload filling every stat from the bus and writing only in `commit()`, `attacker_id` on `player_hit` and `player_died` (a body's def id, a bolt's `shooter_id`), `FavourRules.band_name`, the profile scratch path in `SceneSuite.before_test`/`after_test`. 508 tests green twice, `check_boot` ok; nothing visible changed, so no smoke scenario. Next: Task 5 (the fall and the verdict), after the Task 4 reviews.
- 2026-09-25: Task 4 review pass landed in `ace212e` (see the "Task 4 review" deviation). 514 tests green twice, `check_boot` ok, `smoke.sh idle` ok. Next: Task 5.
- 2026-09-25: Task 5 landed in `3a8610f`: `player_fell`, the gladiator laid flat and the hush, `VerdictRules`, `ThumbSign` over the box, `verdict_given`/`run_ended`, the banking and the run's record through `Profile.commit()`, `GateScreen` (`scenes/ui/gate_screen.tscn`) in place of `Summary`, the yield on R, Restart, and Quit to title, the `verso` and `dives` cheats, `RunState.hits_taken`, `Save.total`, the four sounds' rows, the smoke scenario `fall`, the reload probe on its own scratch profile. 530 tests green twice, `check_boot` ok, `smoke.sh fall` ok (`reports/smoke_fall_verdict.png`: the thumb over the box, the gladiator flat; `smoke_fall.png`: the gate screen). Next: Task 6 (the grounds), after the Task 5 reviews.
- 2026-09-25: Task 5 review pass landed in `881b4f4` (see the "Task 5 review" deviation). 534 tests green twice, `check_boot` ok, `smoke.sh fall` ok. Next: Task 6.
- 2026-09-25: Task 6 landed in `d953898`: `TrainingRules`, `Grounds` (`scenes/grounds.tscn`) with the `Station` areas for the post, the rack, and the gate, `TrainingPanel` and `ArmouryPanel`, `Build.starting` and `RunState.start_run` from the profile's training, Main's one stage slot (`enter_arena`/`enter_grounds`, `_start_run`, `_pass_gate` into the grounds, the gate station into the run), the first-run rule on `flags.returned` (`Save.set_flag`), `grounds_entered`/`training_bought`/`purchase_denied` with `buy`, `buy_denied`, and `music_grounds`, `Player.revive`, `SceneSuite.wait_until`, the reload probe through the gate, the smoke scenario `grounds`, the Roar coins test seeded. 560 tests green twice, `check_boot` ok, `smoke.sh grounds|fall|title` ok (`reports/smoke_grounds.png`: the three stations and the training panel's three rows, no caption). Next: Task 7 (close the milestone), after the Task 6 reviews.
- 2026-09-25: Task 6 review pass landed in `4d8a4cd` (see the "Task 6 review" deviation). 563 tests green twice, `check_boot` ok, `smoke.sh grounds` ok. Next: Task 7.
- 2026-09-25: Task 7 landed as the release commit (tag `m5-candidate`): `docs/TESTERS.md` rewritten under "show, don't tell", `docs/STATUS.md`, `CLAUDE.md`, the M5 feel checklist, the design docs' closing lines, `config/version` 0.5.0-rc1, the grounds suite's two tidy-ups. 563 tests green twice, `check_boot` ok, all eleven smoke scenarios ok; `tools/build.sh` and `tools/release.sh` published the pre-release `v0.5.0-rc1`. Next: the user's playtest against `docs/plans/2026-09-25-m5-feel-checklist.md`; the milestone closes on their verdict (`git tag m5`).
- 2026-09-25: playtest 1 of `0.5.0-rc1` (three runs) returned sixteen notes, recorded in the checklist with a decision each; four design questions settled with the user (UI may name, never narrate; the training lines buy what a run cannot give; a wordless build-up now and the announcer in M6; favour harder to gain with a decay). Tasks 8 to 12 added above. Next: Task 8.
- 2026-09-26: Task 8 paused on the user's request after two of its eleven notes: note 14 in `6d861b1` (`Hud._on_run_started` refreshes the strip; `SceneSuite.after_test` waits a frame after its `start_run`, since the strip's old children are queued frees when gdUnit counts orphans) and note 4 in `3eb4cf1` (the boss numbers, the pinned test, the docs' smoke line, and the charge test's player standing past the longer charge's reach: at 380 px/s the boss reached the body; the commit also carried the `verdict_roll` credit rows from another session's working tree). 565 tests green. Next: Task 8, resume at note 7 (then 8, 10, 15, 16, 6, 13, 1, 2).
- 2026-09-26: Task 8 finished: the nine remaining notes landed one commit each, in the task's order: note 7 `82bcc56` (no verdict on a win; the fanfare on `run_won`), note 8 `bb38986` (the money held on the training panel), note 10 `b7fdcff` (`TrainingRules.LINES[*].text` and the row's line), note 15 `c66e1fe` (`EnemyDef.display_name`, `GateScreen.portrait_line`), note 16 `224dcab` (the boot and the mask beside the pips and the meter; `favour` on the Raven table; `tools/icon_sheet.gd` repaired), note 6 `067c602` (the 24x24 fist at 2x), note 13 `847e5b1` (the fourth card slides in after `FOURTH_CARD_DELAY` with `card_revealed`), note 1 `5ecc413` (the pull, `RunState.pull_radius`), note 2 `e68d02d` (`PILE_WAIT_CAP`). 581 tests green twice, `check_boot` ok, `smoke.sh fall|boss|pick|kill|grounds` ok (`smoke_fall_verdict.png` the fist over the box, `smoke_grounds.png` the panel with the money and the three lines, `smoke_kill.png` the HUD's icons; the four-card picker captured with a temporary scenario). Next: Task 9 (the favour retune, note 3), then Tasks 10 to 12.
- 2026-09-26: Task 8 review pass landed in `f6d5f65` (see the "Task 8 review" deviation: the sheet's thumbs, the two-heads favour placeholder, the pile ring past the pull, the dash row under the hearts' real height, and the minors). 583 tests green twice, `check_boot` ok, `smoke.sh fall|kill` ok (`smoke_fall_verdict.png`: the white thumb up over the box; `smoke_kill.png`: the boot flush under the hearts, the two heads beside the meter). Next: Task 9 (the favour retune, note 3), then Tasks 10 to 12.
- 2026-09-26: Task 9 landed in `815cef8` (the favour retune: `FavourRules.ACTS` kill 1, chain 2, daring 4, clean round 10, hit -25, `START` 20; the decay `DECAY_GRACE` 3 s and `DECAY_PER_SECOND` 1.5 since the last scoring act, `DECAY_ACT` "decay", replacing the cowardice drain and its `enemy_hit` detector; see the "Task 9" deviation). 584 tests green twice, `check_boot` ok, `smoke.sh pick` ok. Next: Task 10 (the verdict's build-up, note 5), then Tasks 11 and 12.
